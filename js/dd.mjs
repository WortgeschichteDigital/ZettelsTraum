
export { dd as default };

const dd = {
  // properties regarding the app
  // (as received from the main process)
  app: {},

  // contents of the current cardbox file
  file: {},

  // properties regarding the current window
  // (as received from the main process)
  win: {},
};
